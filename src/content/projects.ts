import { Project } from "./types";

/**
 * Projects archive for Hoskey Production.
 *
 * This is intentionally initialized as an empty array.
 * Site sections and portfolio grids render marked placeholder Frames
 * until real client projects, showreels, and verified case studies are populated.
 * Do not invent mock projects.
 */
export const projects: Project[] = [];
