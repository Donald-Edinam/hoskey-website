import { TeamMember } from "./types";

/**
 * Team members directory for Hoskey Production.
 * Initialized as an empty array until roster entries are officially published.
 */
export const team: TeamMember[] = [];
